repos:
  - repo: local
    hooks:
      - id: sync-references
        name: Check generated skill references
        entry: mise run sync-references:ci
        language: system
        pass_filenames: false

      - id: format-ci
        name: Check Markdown formatting
        entry: mise run format:ci
        language: system
        pass_filenames: false
